POSTGRES_DSN=postgresql://postgres:postgres@localhost:5432/career_copilot

# Azure AI Search (job RAG index). First run of scripts/rag_index/run.py creates the index if missing.
# AZURE_SEARCH_ENDPOINT=https://your-service.search.windows.net
# AZURE_SEARCH_API_KEY=your-admin-key
# AZURE_SEARCH_INDEX_NAME=career-copilot-jobs
# AZURE_SEARCH_USER_INDEX_NAME=career-copilot-user-profiles

# OPENAI_API_KEY=...  # embeddings (text-embedding-3-large) for jobs and user profiles in Azure AI Search
